<UserControl x:Class="RomForge.Controls.Switch.KeygenTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:NSW.WPF.UI;assembly=NSW.WPF" 
             xmlns:l="clr-namespace:NSW.Core.Properties;assembly=NSW.Core" 
             xmlns:local="clr-namespace:RomForge.Controls.Switch"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <core:FileManagerControl x:Name="fileMgr" Grid.Row="0" Margin="5,0,5,5" 
                                     IsEnabled="{Binding IsIdle}"/>

            <core:IconButton Grid.Row="1"
                             x:Name="btnStartWork" Margin="5,0,5,5"
                             DefaultText="{x:Static l:Resources.Button_StartWork}"
                             DefaultIcon="/Assets/Images/Keygen.png"
                             CancelText="{x:Static l:Resources.Button_Cancel}"
                             CancelIcon="/Assets/Images/Cancel.png"
                             IsRunning="{Binding IsLocked}" 
                             Click="BtnStartWork_Click"/>

            <Grid Grid.Row="2" Margin="5,0,5,5" x:Name="progressArea" Visibility="{Binding IsLocked, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="5,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" x:Name="progressLabel" Text="{Binding ProgressLabel}" FontSize="11" Foreground="#A0A0A0" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1" x:Name="progressPercent" Text="{Binding ProgressPercent}" Margin="10,0,15,0" FontSize="11" Foreground="#4CAF50" VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="progressSpeed" Text="{Binding ProgressSpeed}" Foreground="Gray" FontSize="11" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="progressTime" Text="{Binding ProgressTime}" FontSize="11" Foreground="#4CAF50" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <ProgressBar Grid.Row="1" x:Name="progress" Value="{Binding ProgressPct}" Minimum="0" Maximum="100" Height="4">
                    <ProgressBar.Template>
                        <ControlTemplate TargetType="ProgressBar">
                            <Grid>
                                <Border Background="#25FFFFFF" CornerRadius="2"/>
                                <Border x:Name="PART_Track" CornerRadius="2"/>
                                <Border x:Name="PART_Indicator" Background="#4CAF50" CornerRadius="2" HorizontalAlignment="Left"/>
                            </Grid>
                        </ControlTemplate>
                    </ProgressBar.Template>
                </ProgressBar>
            </Grid>

        </Grid>
    </Grid>
</UserControl>